# This is a PLACEHOLDER. Apply the real secret manually or via Sealed Secrets.
# kubectl create secret generic postgres-secret \
#   --from-literal=DB_PASSWORD=yourpassword \
#   --namespace todo-app
#
# Then encode with kubeseal for GitOps storage.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: {{ .Values.namespace }}
type: Opaque
# Values injected at deploy time — NOT stored in Git
